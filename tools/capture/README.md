# Capture Tools

These scripts capture Zeta reference screens with headed Chrome and Win32 window screenshots.

## Commands

```powershell
npm install
npm run capture:guest
npm run capture:auth
npm run capture:doc
```

Browser profiles are intentionally ignored because they can contain cookies and session state.

