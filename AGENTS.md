# Team Codex Instructions (Essentials)

이 프로젝트에서 Codex가 반드시 따라야 할 핵심 규칙입니다.

## 컨저 / 심플컨저 / 풀컨저 / 컨파

사용자가 "컨저", "심플컨저", "풀컨저", "컨파", "오늘 여기까지"라고 말하면 `context-carryover` 스킬을 사용해 세션 맥락을 보존합니다.

- 모든 컨저 산출물은 기본적으로 한국어로 작성합니다.
- 대상: `chat_log/*.txt`, `chat_log/*_digest.md`, `CONTEXT.md`, `HANDOFF.md`, `NEXT_SESSION.md`.
- 파일명, 명령어, 코드 식별자, 라우트, 테이블명, 컬럼명, 오류 메시지, 원문 고유명사는 원문을 유지할 수 있습니다.
- 원본 trace/jsonl은 번역하지 않습니다. 원본 보존 목적의 raw trace는 있는 그대로 둡니다.
- 정확한 transcript 추출이 불가한 경우에도 "보이는 대화와 도구 실행 기록을 바탕으로 재구성함"이라고 한국어로 표시합니다.
- `chat_log/`는 gitignore 대상이어야 하며, raw trace나 credential은 외부 업로드하지 않습니다.

## 코드 raw 출력 억제

파일 본문, 코드블록, stdout raw, diff를 채팅에 길게 출력하지 않습니다. 변경 내용은 한국어 1~2줄로 요약하고 산출물은 file:// 링크로 보고합니다. 사용자가 "코드 보여줘"라고 명시하면 예외입니다.

## 로컬 경로 링크

로컬 경로는 가능하면 file:// 링크로 보고합니다. 공백은 `%20`으로 처리하고 한글은 원문을 유지합니다.

## Git / 안전

- 다른 개발자의 변경을 되돌리지 않습니다.
- destructive git 명령은 사용자가 명시적으로 요청한 경우에만 실행합니다.
- API 키, token, cookie 등 민감정보를 파일이나 채팅에 쓰지 않습니다.
